{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Jim Simon <jim.j.simon@gmail.com>",
  "license": "Apache-2.0",
  "engines": {
    "npm": "~5.0.0"
  },
  "dependencies": {
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "pg": "^6.2.3",
    "sequelize": "^3.30.4",
    "umzug": "^2.0.1"
  },
  "devDependencies": {
    "sequelize-cli": "^2.7.0"
  },
  "scripts": {
    "devSetup": "npm run migrateDatabase; npm run seedDatabase",
    "migrateDatabase": "docker-compose run --rm api npm run sequelize db:migrate",
    "seedDatabase": "docker-compose run --rm api npm run sequelize db:seed:all",
    "start": "node index.js",
    "sequelize": "node node_modules/.bin/sequelize"
  }
}
