version: '3'
services:
  ui:
    build:
      context: ui
    volumes:
      - ./ui:/usr/src/app
  api:
    build:
      context: api
    volumes:
      - ./api:/usr/src/app
    environment:
      - NODE_ENV=development
    depends_on:
      - db
  db:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=cordcutting
  proxy:
    build:
      context: proxy
    ports:
      - "80:80"
      - "443:443"
