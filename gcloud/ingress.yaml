apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: fanout-ingress
spec:
  tls:
    - secretName: sslsecret
  rules:
  - http:
      paths:
      - path: /api/*
        backend:
          serviceName: api
          servicePort: 3000
      - path: /*
        backend:
          serviceName: ui
          servicePort: 8080
